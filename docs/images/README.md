# Screenshots

Four images, referenced from the project README (currently commented out —
uncomment the block once these exist).

| File | What to capture |
|---|---|
| `queue.png` | Priority queue with the four status counts and several ranked patients |
| `patient.png` | A patient record: collapsed detail panel, last encounter in SOAP |
| `ai-usage.png` | Agent activity, AI usage panel expanded |
| `audit.png` | Audit trail with the chain-intact banner |

Take them at a desktop width against the demo population, so no real patient
data can appear. The demo banner in the screenshot is a feature, not a flaw —
it shows a reader that the data is fictional.
